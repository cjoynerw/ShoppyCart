<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item list</title>
</head>
<body>
    <h1> Shoppy Cart </h1>
    <h2><%= storeID %> </h2>
    <img src=<%= "/img/" + storeID + ".png" %> />
    <form action="/items<%= items.id %>?_method=PUT" method="POST">
        Item Name: <input type="text" name="name" value="<%= items.name %>" required /><br />
        Quantity: <input type="number" name="quantity" value="<%= items.quantity %>" /><br />
        Unit of measure: <input type="unit" name="unit" value="<%= items.unit %>" /><br />
        Additional Info: <input type="notes" name="notes" value="<%= items.notes %>" /><br />
        Date Needed By: <input type="date" name="date" value="<%= items.date %>" /><br />
        <input type="submit" value="Submit Changes" />
    </form>
</body>
</html>